CONTACT_TO_EMAIL="bibisventasysserviciosonline@gmail.com"
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_USER=""
SMTP_PASS=""
SMTP_FROM=""

# Cloudflare Turnstile (CAPTCHA para admin login)
PUBLIC_TURNSTILE_SITE_KEY=""

# Supabase (feature flag desactivado por defecto; activar cuando se valide la migración)
PUBLIC_USE_SUPABASE="false"
SUPABASE_URL=""
SUPABASE_ANON_KEY=""
SUPABASE_SERVICE_ROLE_KEY=""

# Martina di Trento — sync de campaña (SOLO SERVER, nunca PUBLIC_*)
# MARTINA_SYNC_APPLY_ENABLED="true"
# SYNC_PREVIEW_SECRET=""

# Nuvex — sync de catálogo desde el panel admin (SOLO SERVER, nunca PUBLIC_*)
# Credenciales de la cuenta Nuvex usadas para login server-side (no se envían al navegador).
NUVEX_USER_EMAIL=""
NUVEX_USER_PASS=""
# Habilita la escritura (apply) del sync Nuvex. Fail-closed: si no es "true", solo preview.
# NUVEX_SYNC_APPLY_ENABLED="false"
# SYNC_PREVIEW_SECRET se reutiliza para firmar los previews de Nuvex (misma var que Martina).
